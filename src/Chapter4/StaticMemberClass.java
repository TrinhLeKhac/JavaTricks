package Chapter4;

/**
 * Declare a member class that does not require access to an enclosing instance,
 * put the static modifier in its declaration, if not, instance has a hidden reference to its enclosing instance
 * Store this reference take time and space
 * */

public class StaticMemberClass {

}
